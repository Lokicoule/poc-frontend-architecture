import { CognitoUserAttribute } from "amazon-cognito-identity-js";

export const userAttributesValidationBuilder = (): CognitoUserAttribute[] => {
  const validationData: CognitoUserAttribute[] = [];
  return validationData;
};
